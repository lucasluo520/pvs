version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - checkout
      - run:
          name: "Testing"
          command: echo "Hi"
      - run:
          command: |
            echo Running test
            mkdir -p /tmp/test-results
            make test

workflows:
    first job:
      jobs:
        - build